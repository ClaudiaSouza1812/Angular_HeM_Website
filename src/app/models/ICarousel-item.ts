// Interface defines the structure for carousel items
export interface ICarouselItem {
    image: string;   // Property that holds the image path/URL
    title: string;   // Property that holds the title of the slide
}
